import './index.css';

const SearchBar = () => {
    return (
        <div id="searchbar-wrapper">
            <span>?</span>
            <input type="text" placeholder="Search"></input>
        </div>
    );
};

export default SearchBar;
